const state = {   
  title: 'Stone Paper Scissors' 
};

const getters = {
  game: (state) => { return state },   
};

const actions = {
  
};

const mutations = {  
  
};

export default {
  state,
  getters,
  actions,
  mutations,
  namespaced: true,
};